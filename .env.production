# Production Environment Configuration for Railway
ENVIRONMENT=production
DEBUG=false

# Database - Railway PostgreSQL (set these in Railway dashboard)
# DATABASE_URL=postgresql+asyncpg://...
# DATABASE_URL_SYNC=postgresql://...

# Redis - Optional (leave empty for Railway deployment without Redis)
# REDIS_URL=

# TMS Integration
TMS_API_URL=https://gcgc-team-management-system-staging.up.railway.app
# TMS_API_KEY= (set in Railway dashboard)

# Security (generate secure keys in production)
# JWT_SECRET= (set in Railway dashboard - min 32 chars)
JWT_ALGORITHM=HS256

# CORS - Allow your frontend domains
# ALLOWED_ORIGINS= (set in Railway dashboard)

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json